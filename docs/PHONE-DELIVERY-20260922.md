# Phone delivery continuation — September 22, 2026

The owner approved proceeding with all work, including independent review.
This continues the September 19 application and infrastructure candidates.
The live legacy stack is still healthy and execution-locked. No provider job,
execution unlock, phone cutover, release promotion or Hermes reboot has occurred.

## First delivery milestone

Complete one managed read-only task in the owner's selected useful workspace,
then hear a verified result, cancel a running task, resume the phone thread and
observe truthful behavior after interruption. Start with this milestone; the
real handset mutation-approval path remains necessary before enabling mutations.
Do not enable the legacy owner-account controller to obtain a demonstration.

The pending scope question asks for an exact project directory, Anthropic/OpenAI
project names and daily spending limits, without API keys in chat. A second
question asks which narrow public infrastructure repository should own the
independent release workflow. Generic approval does not supply these values.

## Implemented in this continuation

- Root-owned systemd sockets separate the normal controller and PBX emergency
  stop listener. Voice uses the protected Unix socket with no proxy, redirect
  or TCP fallback. Listener activation, lifecycle shutdown and both installers
  carry the same fixed source and unit closure.
- Real controller route matching now agrees with token scope classification.
  Review reproduced incorrect cross-scope handling with Express's previous
  case/trailing-slash defaults. Exact routing rejects those variants; an actual
  server regression proves they leave the execution lock set.
- A resource-admission helper requires an exact root-owned Hermes policy,
  finite aggregate pool, recovery allowance, host/job headroom, zero swap and
  stable pressure/event evidence. It checks before credential registration and
  model start under the existing one-job fence. Missing policy refuses work;
  panic/cancellation remain independent of admission.
- The independent infrastructure candidate installs and reads back the exact
  receiver network policy. Six rule sets passed real nft roundtrips in a
  disconnected anonymous namespace. A separate actual-kernel probe exposed and
  corrected gateway-route ordering while interfaces were down.
- Minimal immutable Docker anchors supply a supported `container:<full ID>`
  namespace attachment path. They retain crash journals, PID/start identity,
  pidfds and namespace handles, with exact task/cgroup identity checks.
  Infrastructure source is compiled directly rather than accepting cached
  Python bytecode. The application now has a protected placement module with
  exact Compose network projection and kernel/Docker observations. The launcher
  refuses activation before cleanup or credentials until the remaining endpoint,
  readiness, egress and coordinated lifetime wiring is complete; see
  [media placement](MEDIA-APPLICATION-PLACEMENT.md).
- A separate PBX stop-only listener is implemented. Installed Asterisk source
  shows that ordinary DTMF events alone cannot attest handset RFC4733 input.
  The real independent adapter and end-to-end stop aggregation remain open;
  see [PBX listener](PBX-PANIC-LISTENER.md) and
  [adapter feasibility](PBX-ADAPTER-FEASIBILITY-20260922.md).
- Both exact pinned provider binaries were acquired from public upstreams and
  verified as data. Claude's manifest signature was verified using its
  documented signing fingerprint. Neither provider was executed or installed.
  The companion infrastructure report records acquisition URLs and hashes.

Independent review identified Linux 6.8 nested PID-event accounting as another
required resource correction. Commit `af24d5a` replaces the aggregate-only
assumption with bounded stable descendant inventories; all 58 focused resource
tests passed and the persistent-descendant correction was independently reviewed.
The next correction adds a mandatory retained raw-inotify observer before the
first sample. It refuses every topology/attribute event, overflow, lost watch,
helper failure or ambiguous protocol outcome, with no timestamp fallback.
Its 70 focused tests passed and the frozen source received independent review.
A separately reviewed, bounded probe on the actual Linux 6.8 kernel confirmed
create/delete events between equal inventories and attribute changes both
invalidate the production observer. It retained no probe directories and
changed no controller settings or process placement. The installed read-only
mount view and observation-to-launch policy still require acceptance.

The receiver renderer and explicit v2 topology now include both required
FreeSWITCH-to-voice connections: media-only HTTP on TCP 3000 and reverse ESL on
TCP 3002. Control HTTP remains loopback-only. The exact v1 contract remains
separate and cannot authorize v2 evidence. Reviewed receiver consumers now bind
the private media HTTP listener and native reverse ESL endpoint to that admitted
projection. Media responses retain and bound the checked file descriptor;
legacy playback and Realtime acknowledgement tones use admitted private URLs.
The root-protected runtime record must bind the current process and namespace,
image and exact source closure. The independent record writer/start transaction,
outbound speech/Realtime connections and live commissioning remain outstanding.

The paired application/host source closure now binds 145 paths, including the
observer, endpoint renderer, transitive SIP topology module and receiver
consumers. Its sorted
path-list SHA-256 is
`a26def1a35870642bf87f459ff02f76dff57625b6e535c81b501fbcd866bbf5d`.
The observer is installed root-owned mode 0555 and invoked through isolated
`/usr/bin/python3 -I`; commissioning must independently bind that Python,
standard-library, `_ctypes` and libc runtime as well as the helper source.

## Next executable delivery steps

1. **Close source integration and review.** Finish outbound voice connections,
   the admitted runtime-record writer and coordinated start/restart lifetime
   checks, then repin the exact release closure. Keep application and
   infrastructure revisions paired.
2. **Commission the shared-host profile.** Turn the reviewed numerical resource
   proposal's reviewed dormant slice/unit limits into commissioned capacity
   with an independent recovery path. Provision
   bounded durable state/workspace filesystems and perform WAL-safe migration
   rehearsal. Measure actual caps with the selected workload. Preserve owner
   session resources and all three FreeSWITCH volumes.
3. **Produce an independently accepted release.** Designate the narrow release
   authority repository; complete scanner database/builder inputs and the
   networkless build/rebuild using the verified npm/Alpine/OCI/provider/tool
   materials; bind actual signer/source/artifact identities.
   Existing persistent CI and dormant fixture signatures cannot approve it.
   Implement the reviewed Hermes read-only environment without changing the
   dedicated-staging installer's deliberate Hermes refusal.
4. **Perform disabled installation and negative acceptance.** Prove identity,
   sockets, resource/storage bounds, receiver separation, provider egress,
   restart gating, recovery and locked-state behavior on the exact release.
   Commission the PBX edge and Realtime/legacy speech-service egress explicitly.
5. **Run the attended useful call.** Capture a fresh identity-bound rollback,
   recheck zero active calls, announce the bounded interruption, and switch
   matched voice/controller versions. Verify inspection, actual provider result,
   speech, cancellation, resume, duplicate handling and restart uncertainty.
6. **Complete the broader phone acceptance.** Finish independent handset
   approval/first-effect enforcement before mutation features, then the full
   attended feature matrix, stop/start recovery, explicit rollback and final
   five-hour trial. Hermes reboot validation remains deferred.

## Current measured evidence

On September 22 the live helper again reported healthy and locked, with the
same three container IDs/images observed September 19 and zero restarts.
Asterisk reported zero active calls/channels and five processed calls. These
are health observations; there is no new useful-phone-task acceptance yet.

At 10:18 UTC root filesystem availability was 36,449,021,952 bytes, about 33.95 GiB,
and `/var/lib/teleagent-voice` still resides on that filesystem rather than a
dedicated mount. This supersedes the September 19 free-space snapshot. The
resource review found about 7.43 GiB MemAvailable and nearly 2 GiB swap occupied;
neither free space nor available RAM constitutes a reservation. A metadata-only
storage scan found 7,696,384 allocated bytes across the scoped state roots and
three distinct FreeSWITCH volumes. The private infrastructure proposal budgets
20.5 GiB on the existing state volume and a provisional 8 GiB workspace on root;
the selected project size, mount commissioning and migration rehearsal are pending.

Validation completed before the final application media-placement follow-up:

- 81 host installation/rollback/lifecycle tests passed in 556.64 seconds.
- 39 release-contract tests and 14 aggregate disabled-installer tests passed.
- The final full controller/voice run passed **887/887**, with no skipped or
  cancelled tests, in 223.07 seconds; peak cgroup memory was 133,996,544 bytes.
  This includes the resource and route-matching corrections.
- The initial full controller/voice run passed 854 of 855 tests. Its only
  failure was the manifest test's outdated helper allowlist. After correcting
  that assertion and the resource sampling fix, all 119 affected tests passed.
- All 17 actual scoped/executor/privileged authentication integration tests
  passed after the routing correction. The first added regression used the
  wrong response field in its final assertion; that test assertion was fixed.
- Root repository lint passed without errors or warnings.
- The media author and independent reviewer both passed the bounded timeout
  and six-policy real-kernel roundtrip checks. Docker anchor tests compile and
  signal the minimal native anchor; they have not started Docker containers.
- The later application media placement candidate passed nine new module tests
  and 24 existing launcher tests, plus focused lint. Its independent review
  accepted the dormant preparation/observation scope and explicit activation
  refusal; it does not establish live creation or restart lifetime proofs.
- After observer/renderer closure integration, 150 affected application tests,
  all 39 release tests and all 14 aggregate disabled-installer tests passed.
  The installer run took 107.93 seconds and peaked at 40,271,872 bytes.
- The receiver-consumer integration ran 934 controller/voice tests in 239.35
  seconds: 933 passed, and the source metadata check correctly refused
  group-writable files created by the checkout umask. Restoring Git's expected
  file modes changed no source bytes; all 11 affected deployment tests then
  passed. Peak full-run memory was 129,343,488 bytes.
- The new receiver files also require `voice-app/package.json` in the bound
  source closure. The release regression caught that omission; adding its
  package scope to both sides gives the exact 145-path set above. All 39
  release tests, three affected host closure tests, 12 resource-policy tests
  and full repository lint passed after the corresponding integration fixes.

Explicit test/probe runs use the shared serial Hermes envelope: 512 MiB memory,
zero swap, 128 tasks and one CPU. Provider data acquisition peaked at 372.7 MB;
tests otherwise remained substantially below the cap. Temporary matching-lockfile
dependency links are source-QA inputs, not production build artifacts.
